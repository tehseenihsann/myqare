{
  "buildCommand": "npm run db:generate && npm run db:seed && npm run build",
  "installCommand": "npm ci --prefer-offline --no-audit || npm install --prefer-offline --no-audit",
  "framework": "nextjs"
}

